{
  "name": "vendoor-connect-website",
  "version": "1.0.0",
  "private": true,
  "description": "Vendoor Connect website — vendor portal backend (Netlify Functions + Netlify Database).",
  "dependencies": {
    "@netlify/database": "latest"
  }
}
